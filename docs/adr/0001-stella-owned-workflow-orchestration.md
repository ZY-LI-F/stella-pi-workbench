# ADR 0001：由 Stella 持有工作流状态

Stella 负责持久化流程实例、依次启动隔离的 Pi RPC 进程并处理人工关卡，Pi 只负责单个 Agent 步骤。这样即使应用重启，任务、步骤和产物仍可解释和恢复，且界面能直接展示真实事件；代价是 Stella 需要维护编排器和进程生命周期，而不能把流程隐藏在一次聊天或一个子 Agent 扩展里。该边界也让固定流程不依赖用户机器上额外安装的扩展。
